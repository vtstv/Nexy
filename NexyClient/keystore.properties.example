# Nexy Client Keystore Configuration Example
# Copy this file to keystore.properties and fill in your actual values
# DO NOT commit keystore.properties to version control!

# Keystore file location (relative to project root)
KEYSTORE_FILE=nexy-release.keystore

# Keystore password - CHANGE THIS!
KEYSTORE_PASSWORD=your_keystore_password_here

# Key alias
KEY_ALIAS=nexy-release-key

# Key password - CHANGE THIS!
KEY_PASSWORD=your_key_password_here

# Instructions:
# 1. Generate a keystore using Android Studio or keytool:
#    keytool -genkey -v -keystore nexy-release.keystore -alias nexy-release-key -keyalg RSA -keysize 2048 -validity 10000
# 2. Place the keystore file in the location specified above
# 3. Update the passwords in this file
# 4. NEVER commit this file to git!
