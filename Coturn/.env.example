# ===========================================
# Coturn Production Environment Variables
# ===========================================
# Copy this file to .env and fill in the values:
#   cp .env.example .env
#   nano .env
#
# NEVER commit .env to git!

# External IP address (REQUIRED for production)
# Your server's public IP address
# Example: EXTERNAL_IP=203.0.113.50
EXTERNAL_IP=your.public.ip.address

# TURN Secret for time-limited credentials (REQUIRED for production)
# Generate with: openssl rand -hex 32
# Minimum 64 characters for security
# This same secret must be configured in NexyServer!
TURN_SECRET=generate_with_openssl_rand_hex_32

# TURN Realm (domain)
TURN_REALM=turn.nexy.app

# Legacy: Static password (for development only, DO NOT use in production)
TURN_PASSWORD=change_this_strong_password_in_production

# Database settings (if using PostgreSQL authentication)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=messenger_db
DB_USER=messenger
DB_PASSWORD=messenger_password

# Redis settings (if using Redis for session management)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# TLS/SSL settings
# Set to 'true' to enable TLS in production
ENABLE_TLS=false

# Monitoring
# Set to 'true' to enable Prometheus metrics
ENABLE_METRICS=false
